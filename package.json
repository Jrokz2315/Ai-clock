{
  "name": "ai-clock",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A public, request-time clock for browsers and AI readers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test worker.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
